{
    "name": "Android SDK",
    "id": "android-sdk",
    "version": "1.0.7",
    "description": "Installs the Android SDK and command line tools.",
    "containerEnv": {
        "ANDROID_HOME": "/usr/local/lib/android",
        "ANDROID_SDK_ROOT": "$ANDROID_HOME",
        "PATH": "$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools"
    },
    "options": {
        "android_home": {
            "type": "string",
            "default": "/usr/local/lib/android",
            "description": "The directory in which to install the Android SDK."
        },
        "sdk_version": {
            "type": "string",
            "default": "36.1",
            "description": "The version of the Android SDK to install."
        },
        "build_tools_version": {
            "type": "string",
            "default": "36.0.0",
            "description": "The version of the Android build tools to install."
        },
        "java_version": {
            "type": "string",
            "default": "25",
            "description": "The version of Java to install."
        },
        "setup_host_sdk": {
            "type": "boolean",
            "default": false,
            "description": "Install an additional SDK for the selected host operating system."
        },
        "host_os": {
            "type": "string",
            "enum": ["linux", "windows", "macosx"],
            "default": "linux",
            "description": "The operating system of the host for the additional SDK."
        },
        "host_sdk_dir": {
            "type": "string",
            "default": "/usr/local/lib/android-host",
            "description": "The directory in which to install the additional host SDK."
        }

    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils:2",
        "ghcr.io/devcontainers-extra/features/apt-get-packages:1"
    ]
}
